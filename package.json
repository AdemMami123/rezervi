{
  "name": "rezervi",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "vercel-build": "npm --prefix client install --legacy-peer-deps && npm --prefix client run build"
  }
}
