{
  "buildCommand": "npm install --prefix client && npm run build --prefix client && cp -r client/build ./build",
  "outputDirectory": "build",
  "installCommand": "echo 'skipping'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}